/**
 * Teardown global para testes E2E
 * Executado uma vez depois de todos os testes
 */

export default async () => {
  console.log('\n✅ E2E tests completed\n');

  // Limpezas globais podem ser adicionadas aqui
  // Exemplo: fechar conexões persistentes, limpar caches, etc.
};
